import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.jamtd.reacttd",
  appName: "React TD",
  webDir: "dist",
  android: {
    path: "android",
  },
};

export default config;
